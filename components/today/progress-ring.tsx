/**
 * The Today completion ring now uses the shared, motion-enabled data-viz
 * primitive (violet secondary accent, tabular mono readout). Re-exported here so
 * existing import paths keep working.
 */
export { ProgressRing } from "@/components/ui/progress";
